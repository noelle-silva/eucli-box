# 更新记录

## 0.1.0 - 2026-07-26

- 建立 eucli-box 独立产品版本，并在启动信息和发布查询中显示。
- 建立客户端、AI 工具和系统插件统一的三段版本与适用范围判断。
- 不适用工具和插件继续显示但停止使用；不适用客户端保留连接设置并阻止正常业务。
- 将工具本体与长期工具数据物理分开，并完成现有资料的一次性核对整理。
- 新增专用版本检查与调整命令，统一核对版本、适用范围和中文文档。
- 建立业务端、7 个 AI 工具和 3 个系统插件各自独立的 Windows x64 正式成品制作与验收能力。
- 建立固定官方来源、未公开上传、远端回读核对、公开和公开后复核的正式发布流程。
- 新增业务端自动版本检查：只读取公开发行资料，区分无新版、发现新版和检查失败，不自动下载或安装。
- 新增隔离的阶段验证入口，验证现场统一保存在 `.release/verification/`，成功后只保留脱敏证据。
- 将验证成果与隔离现场收尾分开报告：自动清理受本机限制时明确保留人工清理位置，不再推翻已经通过的成果和安全检查。
- 新增本地发布配置 `.release/config/github.env` 的三类 GitHub 凭据读取规则，业务端、AI 工具和系统插件分别使用固定字段，凭据文件不进入源码记录。

## 未发布

- 优化客户端思考过程与异步工具返回视觉：思考过程改为白色卡片，输出中自动展开、输出完成后默认收起；异步工具返回不再使用蓝色半透明外观。
- 新增客户端配色预设系统：可在“设置 → 外观”中切换内置配色，也可粘贴 JSON 导入自定义配色预设。
- 新增多套客户端默认纯色配色：包含“晨雾蓝”“暮樱墨”“云朵马卡龙”“苔林绿”“蜜桃粉”“紫藤雾”和“羊皮纸”，主要背景和底部输入发送组件保持白色旧视觉，弹层和设置页的局部层级会跟随当前配色刷新。
- 新增 AI 微服务模型组选择：上下文压缩、Mermaid 修复、聊天记录取名和表情包取名都可选择模型组中的对外模型。
- 修复设置页 AI 微服务模型配置展示：供应商选择项显示供应商名称，不再把内部编号直接展示给用户。
- 优化客户端会话记录弹层：历史会话按每批 20 条分批展示，滚动接近底部四分之一区域时继续追加下一批，避免一次性渲染大量会话。
- 优化客户端聊天消息视觉：用户消息改为白色气泡，不再使用透明蓝色外观、顶部身份标签或轮廓线装饰。
- 优化客户端工具展示：工具调用与工具返回放入同一个默认收起的工具容器，标题直接显示工具名称，展开后仍分别查看调用内容与返回内容。
- 优化客户端设置页视觉：设置分区统一改为柔和外壳、柔和分区、无轮廓表单和底色层级，不再主动使用分割线或描边来做布局分组。
- 优化客户端模态窗与小弹层视觉：普通弹窗、角色/群组/工作区选择、临时模型和聊天记录弹层统一使用柔和外壳、无轮廓表单和柔和列表条目。
- 修复设置页 AI 工具“查看/配置”空白问题：工具参数说明复用设置页统一字段读取能力，不再因字段说明读取缺失导致页面崩溃。
- 优化客户端页面滚动视觉：主要页面滚动区统一使用自绘滑动条，不再显示浏览器默认滑动条轨道。
- 新增系统插件子系统：支持从 `system-plugins/` 读取本地插件声明，并在 `data/system-plugins/` 保存用户配置和占位符接口改名。
- 新增插件动态占位符：用户可从插件接口创建占位符，发送前由插件提供动态文本值，再交给占位符系统统一替换。
- 新增系统插件管理页：可查看插件列表、插件状态、占位符接口、接口改名和插件用户配置。
- 新增系统插件缓存心跳型运行方式：业务端可保存插件最近结果，并按心跳节奏主动更新。
- 新增占位符管理页插件入口：可列出尚未创建到占位符空间的插件接口，并一键创建对应占位符。
- 增加插件失败降级：插件启动或取值失败不会阻断整体运行，对应占位符保持原样并进入问题看板。
- 新增内置时间插件：可从插件接口创建 `{{time}}` 占位符，并通过预设选项配置当前时间的格式和时区。
- 新增内置天气信息插件：可通过和风天气提供 `weather_detail` 详细天气和 `weather_Brief` 当前天气简报，并使用缓存心跳型运行方式。
- 新增内置系统信息插件：可提供 `system_info` 当前平台和系统运行信息，不输出当前工作目录。
- 新增独立占位符子系统：可在设置页维护全局占位符，并在提示词中使用 `{{名字}}` 引用。
- 新增占位符发送前替换：角色、工作区、群组和 hook 提示词进入 AI 前会临时替换，占位符不会写回原始保存内容。
- 新增占位符递归展开和问题识别：支持占位符值继续引用占位符，并识别循环引用与异常重名。
- 新增占位符管理页：支持列表、新建、编辑、删除、收藏夹筛选、解析预览、依赖树和问题看板。
- 调整客户端设置页分区入口：页面内部顶部分类切换栏改为固定左侧分区栏，右侧继续显示当前分区内容并独立滚动。
- 新增异步工具调用：工具请求可指定同步或异步，异步任务先受理并后台执行，真实结果稍后回到当前会话上下文。
- 修正异步工具运行节奏：异步任务受理后不再压住当前流程等待真实结果，后续处理会先继续推进，结果完成后再补回上下文。
- 修正异步工具结果回灌：后台结果完成且当前会话没有运行中的处理时，会自动进入下一轮处理，不再依赖用户再发一条消息触发。
- 修正异步工具完成时机：如果后台结果在 AI 正在输出时完成，当前输出结束后会立刻接入结果并继续处理，不再等用户下一次发言。
- 修正异步工具回灌后的继续表现：后台任务会保留发起时的运行外壳，结果回来后的继续处理与同步工具返回保持一致。
- 修正异步工具回灌后的消息位置：后台结果不再覆盖已有回复，后续回复会接在后台结果之后；会话刷新时按消息接续关系恢复当前路径，避免后台返回变成用户消息或挤掉最新回复。
- 调整异步工具结果身份：异步结果回到会话时使用独立的内部身份，客户端展示为后台工具返回，交给模型前仍复用已有工具结果语义。
- 修正客户端重启后的异步结果身份：运行通知写入当前会话后会同步进入长期记录，避免首次显示正确但重启后恢复成用户消息。
- 修正客户端重启读取异步结果时的身份搬运：长期记录中的后台工具返回会在客户端重启后继续保留后台工具返回身份，不再被中间转换误贴成用户消息。
- 调整工具默认调用方式：工具必须显式声明默认同步或异步，缺少声明会被视为配置不完整，不再暗中按同步处理。
- 新增异步工具任务记录：任务状态跟随会话保存，支持重启后恢复未回灌结果，并标记进程已结束的未完成任务。
- 新增异步工具只读监控入口：聊天输入栏旁展示当前会话异步任务数量，可打开查看任务名、工具名、状态、提交时间和耗时。
- 修正异步工具监控入口数量：输入栏旁的“异步”数字只统计正在运行的后台任务，等待中任务仍可在弹窗列表中查看。
- 调整工具确认等待：等待用户确认时若异步结果到达，会按系统事件打断并标记原因，避免误判为用户拒绝。
- 新增 hook 提示词：可在设置中维护预设，并在当前聊天选择一个预设；发送时由业务端临时加入本轮请求，不写入聊天记录。
- 新增 hook 提示词预设库：预设统一保存在业务端 `data/meta/hook-prompts.json`，聊天只保存当前选中的预设。
- 新增角色默认 hook 提示词：角色可配置长期默认预设，聊天没有单独选择时会自然跟随角色默认。
- 新增 hook 提示词客户端入口：设置页支持管理预设，角色编辑支持配置默认预设，聊天输入区支持跟随角色、指定预设或明确不用，草稿聊天可以先选并在首条发送时由业务端正式落地。
- 调整 hook 提示词会话语义：当前聊天选择“无预设”表示明确不用任何 hook 提示词，会覆盖角色默认预设。
- 调整角色会话目录：角色会话统一迁到 `data/sessions/roles/{roleId}/{sessionId}/`，并已完成一次性手动迁移。
- 新增工作区第三入口：客户端现在支持以工作区作为独立入口进入聊天，再在工作区内选择当前角色发言。
- 新增工作区管理：可在设置中创建、编辑、删除工作区，并维护目录清单与工作区提示词。
- 新增工作区实际提示词展示：工作区设置窗会展示后台生成的实际提示词，修改草稿后可刷新查看最新结果。
- 新增工作区会话闭环：工作区会话已支持创建、切换、发送、改标题、删除、运行状态回流和顶部入口切换。
- 调整工作区会话归属：同一工作区内会按当前角色分开保存、读取和展示会话，输入草稿、当前会话和运行状态提示不再跨角色共用。
- 调整工作区消息操作路径：工作区会话的标题、消息编辑、删除和子树删除都会落到当前角色对应的会话记录。
- 完成开发数据一次性迁移：旧的工作区会话目录已搬到新的工作区角色分层目录，并补齐对应索引清单。
- 新增工作区上下文与路径围栏：工作区目录和提示词会进入对话上下文，文件与命令工作目录会先经过工作区范围确认，再进入角色工具权限确认。
- 接入群聊真实运行闭环：群组会话已支持发送、继续回复、重新生成、停止和运行状态刷新。
- 收口当前会话设置边界：临时模型和思考等级只属于角色聊天，群组输入栏不再展示、保存或透传这些设置。
- 修正群聊会话选择与同步：群聊点选、改名、删除和低频同步都按群聊目标独立处理。
- 新增 AI 工具提示说明编辑：可在客户端工具配置中为单个工具填写用户版本说明，AI 识别工具时优先使用用户版本。
- 新增 AI 工具提示说明恢复默认：清空用户版本后，工具会重新使用工具包自带默认说明。
- 新增手动 `/compact` 上下文压缩：用户可主动把较早会话整理为摘要，后续上下文使用“最新摘要 + 最近原文”。
- 新增上下文压缩专用 AI 微服务配置：可在设置中选择压缩模型，并配置压缩后保留的最近原文条数。
- 新增压缩系统记录展示：聊天区和消息树能识别压缩边界与上下文摘要，不再把它们当普通聊天消息处理。
- 增加压缩相关保护：压缩系统记录和已被摘要覆盖的历史消息不能被普通编辑或删除。
- 增加斜杠命令校验：未知命令不会静默进入普通聊天，`/compact` 不接受参数和附件。
- 接入当前会话临时模型真实运行路径：当前聊天可保存临时模型，运行时优先使用该选择，角色默认模型不被改写。
- 统一当前会话设置落稳规则：临时模型和思考等级保存完成后再允许发送、继续回复或重新回复。
- 移除当前会话临时模型未接入的主路径阻断提示，并保留角色聊天专属的临时模型保存路径。
- 收敛 Mermaid 修复职责：修复、替换和保存只由 e-b 完成，客户端不再二次保存消息。
- 调整 Mermaid 修复后的显示流程：客户端等待 e-b 完成后重新读取最新会话，再刷新页面显示。
- 清理 Mermaid 修复成功态的临时替换表现，避免页面先替换、失败回退、再同步回来的抖动。
- 调整重新回复行为：基于目标回复之前的完整聊天上下文重新生成，不再要求上一段必须是用户消息。
- 保留重新回复的新路线效果：新回复不会覆盖旧回复，仍按现有路线分支方式保存。
- 增加从已有上下文继续生成的运行入口校验与回归测试。
- 移除模型请求过程的默认流水落盘，避免 `data/meta/` 持续积累请求小文件。
- 保留正常模型请求、聊天、标题生成、图表修复和表情命名等主流程能力。
- 明确 `data/meta/` 更适合长期辅助信息，不再作为请求流水的堆积位置。
