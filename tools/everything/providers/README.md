# everything provider assets

This directory is populated with official voidtools Everything runtime assets when building the tool package.

Expected files:

- `Everything.exe`
- `es.exe`
- `License.txt`
- `THIRD_PARTY_NOTICES.md`
- `vendor-manifest.json`

Build with the `everything-root` asset root to copy the verified vendor directory into the runtime tool package.
